```c++
注意aac格式是安卓要求支持的格式，如果取消支持aac，会无法通过google CTS测试

frameworks/av/media/libstagefright/MediaCodec.cpp 文件

diff --git a/media/libstagefright/MediaCodec.cpp b/media/libstagefright/MediaCodec.cpp
index 96f238b..3a52d52 100644
--- a/media/libstagefright/MediaCodec.cpp
+++ b/media/libstagefright/MediaCodec.cpp
@@ -1147,6 +1147,11 @@ static const CodecListCache &GetCodecListCache() {
 }

 status_t MediaCodec::init(const AString &name) {
+    if (!strcmp(name.c_str(),"OMX.google.aac.decoder")
+        || !strcmp(name.c_str(),"c2.android.aac.decoder")){
+        ALOGI("not support aac decoder");
+        return NAME_NOT_FOUND;
+    }
     mResourceManagerProxy->init();

     // save init parameters for reset
```

